# kabutan-material

GitHub Pages + GitHub Actions で株材料一覧を表示する最小構成です。

## 使い方
1. この中身を GitHub リポジトリにアップロード
2. Settings > Pages で `main` ブランチ `/docs` を指定
3. Actions の `Update materials` を実行
4. Pages URL で一覧を確認
